<% @users.each do |e| %>
<p>
    Uid: <%= e.uid%>
    Pass: <%= e.pass%>
    Age: <%= e.age%>
    <%=link_to "削除", user_path(e),data: { "turbo-method": :delete } %>
</p>

<%end%>


<p><%=link_to "新規追加", new_user_path%></p>

<hr>

<%=link_to "ツイート一覧", tweets_path%>
